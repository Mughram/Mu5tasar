# Reference only: configure these in the PHP host's environment/secret settings.
# This project deliberately does not load a .env file from the web directory.
# Project: nluvqdwcouoloynplhjq
# Verified in Connect > Direct > Session pooler on 2026-09-15 (IPv4).
DB_HOST=aws-0-ap-northeast-1.pooler.supabase.com
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres.nluvqdwcouoloynplhjq
DB_PASSWORD=
DB_SSLMODE=verify-full
# Absolute path outside the web root to the trusted CA downloaded from Supabase.
DB_SSLROOTCERT=
# Keep 1 on HTTPS production; 0 only for local HTTP development.
SESSION_COOKIE_SECURE=1
