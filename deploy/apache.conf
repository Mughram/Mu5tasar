<VirtualHost *:${PORT}>
    DocumentRoot /var/www/mu5tasar/main

    <Directory /var/www/mu5tasar/main>
        Options -Indexes
        AllowOverride All
        Require all granted
    </Directory>

    ErrorLog /proc/self/fd/2
    CustomLog /proc/self/fd/1 combined
</VirtualHost>
